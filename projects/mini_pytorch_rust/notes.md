# notes — mini_pytorch_rust

Daily log. One entry per session.

---

## Template

### YYYY-MM-DD (Phase N)

**Built:**
- ...

**Stuck on:**
- ...

**Next session:**
- ...

---

## Sessions

### 2026-06-09 (Phase 0 — planning)

**Built:**
- Project scaffolded: `README.md` with 7 phases + milestones, `src/` empty.

**Next session:**
- Pick a Rust workspace location (likely a separate repo or this `src/` initialized as a cargo project).
- Do the first feeding movement: `rust_tensor_struct` — `Tensor { data: Vec<f32>, shape: Vec<usize>, stride: Vec<usize> }` plus indexing and `Tensor::zeros`. ~60-90 min.
- Decide on the Rust workflow: edit in this repo OR have the project be a sibling Cargo workspace.
